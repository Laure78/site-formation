/**
 * FAQ SSR — tout le texte des réponses est dans le DOM (alignement FAQPage JSON-LD).
 * Réexport pour les imports existants `@/components/landing/FAQSection`.
 */
export { FAQSectionServer as FAQSection } from '@/components/landing/FAQSectionServer';
