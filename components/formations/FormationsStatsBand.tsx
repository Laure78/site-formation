import { ProofStats } from '@/components/ProofStats';

/**
 * Bande stats compacte sous le hero — délègue à `<ProofStats />`.
 */
export function FormationsStatsBand() {
  return <ProofStats showQualiopi />;
}
